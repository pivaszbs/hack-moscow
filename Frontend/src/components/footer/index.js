import React from 'react';
import './footer.sass';
import { connect } from 'react-redux';

const Footer = () => {
	return <footer className="footer">The Taco team production present</footer>;
};

export default Footer;
